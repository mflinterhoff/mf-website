/**
 * Canonical production origin. Single source of truth for absolute URLs used in
 * the sitemap, robots.txt and JSON-LD structured data. Keep in sync with the
 * `metadataBase` in app/layout.tsx.
 */
export const SITE_URL = 'https://matthiasflinterhoff.com'
